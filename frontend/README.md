# 짜조 프론트엔드

React + TypeScript + Vite 기반의 AI 일정 플래너 프론트엔드

## 🚀 시작하기

### 패키지 설치
```bash
npm install
```

### 개발 서버 실행
```bash
npm run dev
```

브라우저에서 http://localhost:5173 접속

### 빌드
```bash
npm run build
```

## 📁 프로젝트 구조

```
src/
├── components/     # 재사용 가능한 UI 컴포넌트
├── pages/         # 페이지 컴포넌트
│   └── ApiKeyPage.tsx
├── stores/        # Zustand 상태 관리
│   └── apiKeyStore.ts
├── services/      # API 통신 서비스
│   └── apiKeyService.ts
├── types/         # TypeScript 타입 정의
│   └── api.ts
├── utils/         # 유틸리티 함수
│   └── cn.ts
├── App.tsx        # 루트 컴포넌트
├── main.tsx       # 엔트리 포인트
└── index.css      # 글로벌 스타일
```

## 🎨 기술 스택

- **React 18** - UI 라이브러리
- **TypeScript** - 타입 안정성
- **Vite** - 빌드 도구
- **Tailwind CSS** - 유틸리티 CSS
- **Zustand** - 상태 관리
- **React Router** - 라우팅
- **Lucide React** - 아이콘

## 🔐 보안

- API 키는 sessionStorage에만 저장
- 브라우저 종료 시 자동 삭제
- 서버에 API 키를 전송하지 않음 (BYOK 방식)
