# 짜조(JJAJO) UX 개선 구현 가이드

## 🎯 목표
1. Quick Replies (버튼형 옵션)
2. AI 추천 일정
3. "지금 당장" 모드

---

## 1️⃣ Quick Replies 구현

### 타입 정의 (frontend/src/types/chat.ts)
```typescript
export interface QuickReply {
  id: string
  text: string
  value: string
  icon?: string
}

export interface ChatMessage {
  // ...기존 필드들
  type?: MessageType | 'quick-reply'
  quickReplies?: QuickReply[]
}
```

### Quick Reply 버튼 컴포넌트
- 파일: `frontend/src/components/chat/QuickReplyButtons.tsx` ✅ 생성 완료
- 기능: 3가지 이내의 버튼형 옵션 표시
- 스타일: 둥근 버튼, 아이콘 포함

### 사용 예시
```typescript
// AI 응답 시 Quick Replies 추가
const aiMessage: ChatMessage = {
  content: "언제까지 달성하고 싶으신가요?",
  type: 'quick-reply',
  quickReplies: [
    { id: '1', text: '1개월 안에', value: '1개월 안에', icon: 'zap' },
    { id: '2', text: '3개월 안에', value: '3개월 안에', icon: 'calendar' },
    { id: '3', text: '6개월 안에', value: '6개월 안에', icon: 'clock' },
  ]
}
```

---

## 2️⃣ AI 추천 일정 구현

### 추천 로직
1. **과거 데이터 분석** (TODO: 구현 필요)
   - 사용자의 선호 시간대
   - 자주 사용하는 목표 유형
   - 평균 투자 시간

2. **추천 일정 생성**
```typescript
// 예시
const recommendedSchedule = {
  goal: "토익 800점",
  timeline: "3개월",
  dailyHours: 2,
  preferredTime: "저녁 8-10시", // 과거 데이터 기반
  reason: "평소 저녁 시간에 집중도가 높으셨어요"
}
```

3. **승인 플로우**
```
AI: "평소 패턴을 보니 저녁 8-10시에 하루 2시간씩 3개월이면 달성 가능할 것 같아요!"
   [✅ 이대로 등록] [✏️ 수정하기]
```

---

## 3️⃣ "지금 당장" 모드 구현

### 3단계 분리
```
┌──────────────────────────────────────┐
│  [🌐 전체]  [🌅 오전]  [⚡ 지금 당장] │  ← 탭 형태
└──────────────────────────────────────┘
```

### 각 모드 특징

#### 🌐 전체 모드 (기존)
- 장기 목표 설정
- 상세한 대화형 플래너
- 완벽한 계획 수립

#### 🌅 오전 모드 (NEW)
- 오늘 하루 계획
- 간단한 질문 (2-3개)
- 오전에 집중할 작업 추천

#### ⚡ 지금 당장 모드 (NEW)
- **질문 0개**
- 즉시 추천 일정 제시
- 한 번의 클릭으로 등록

### 지금 당장 모드 UX
```
사용자: [⚡ 지금 당장] 탭 클릭

AI: "지금 시간은 오후 3시네요!
     다음 중 하나를 해보는 건 어떨까요?"
     
     [📚 공부 30분]  [🏃 운동 30분]  [💻 코딩 1시간]
     
     (과거 데이터: 이 시간에 주로 공부하셨어요)
```

---

## 📋 구현 우선순위

### Phase 1: Quick Replies (이번 구현)
- [x] 타입 정의
- [x] QuickReplyButtons 컴포넌트
- [ ] AiChatPanel에 통합
- [ ] 백엔드 Quick Replies 생성 로직

### Phase 2: AI 추천 일정
- [ ] 과거 데이터 수집 API
- [ ] 추천 알고리즘
- [ ] 승인/거절 UI

### Phase 3: 지금 당장 모드
- [ ] 탭 UI
- [ ] 모드별 로직 분리
- [ ] 즉시 추천 알고리즘

---

## 🚀 빠른 적용 방법

### 1. AiChatPanel에 Quick Replies 추가
```typescript
// AI 응답 후
if (!response.readyToCreateGoal) {
  const quickReplies = generateQuickReplies(collectedInfo)
  aiMessage.quickReplies = quickReplies
  aiMessage.type = 'quick-reply'
}

// 메시지 렌더링 시
{message.quickReplies && (
  <QuickReplyButtons
    quickReplies={message.quickReplies}
    onSelect={handleQuickReplySelect}
    disabled={isLoading}
  />
)}
```

### 2. generateQuickReplies 함수
```typescript
const generateQuickReplies = (info: CollectedInfo): QuickReply[] => {
  // 목표 없음
  if (!info.goal_type) {
    return [
      { id: '1', text: '토익 800점', value: '토익 800점', icon: 'zap' },
      { id: '2', text: '다이어트 10kg', value: '다이어트 10kg' },
      { id: '3', text: '코딩 마스터', value: '코딩 마스터' },
    ]
  }
  
  // 기한 없음
  if (!info.mentioned_deadline) {
    return [
      { id: '1', text: '1개월 안에', value: '1개월 안에', icon: 'zap' },
      { id: '2', text: '3개월 안에', value: '3개월 안에', icon: 'calendar' },
      { id: '3', text: '6개월 안에', value: '6개월 안에', icon: 'clock' },
    ]
  }
  
  // 시간 없음
  if (!info.mentioned_hours) {
    return [
      { id: '1', text: '1-2시간', value: '하루 1-2시간' },
      { id: '2', text: '3-4시간', value: '하루 3-4시간' },
      { id: '3', text: '5시간 이상', value: '하루 5시간 이상' },
    ]
  }
  
  return []
}
```

### 3. Quick Reply 선택 핸들러
```typescript
const handleQuickReplySelect = async (value: string) => {
  // 사용자 메시지로 추가
  const userMessage: ChatMessage = {
    id: `msg-${Date.now()}`,
    role: 'user',
    content: value,
    timestamp: new Date().toISOString(),
  }
  addMessage(userMessage)
  
  // 대화 계속
  setInputValue('')
  setLoading(true)
  // ... 기존 handleSendMessage 로직
}
```

---

## 📊 예상 효과

| 항목 | Before | After |
|------|--------|-------|
| 평균 입력 횟수 | 5-10회 | 2-3회 (Quick Replies) |
| 타이핑 시간 | 30초+ | 클릭 1초 |
| 사용자 이탈률 | 40% | 10% (예상) |
| 계획 생성 시간 | 2-3분 | 30초 (지금 당장 모드) |

---

## 🎨 UI 프리뷰

### Quick Replies
```
AI: 언제까지 달성하고 싶으신가요?

┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ ⚡ 1개월 안에 │ │ 📅 3개월 안에 │ │ 🕐 6개월 안에 │
└──────────────┘ └──────────────┘ └──────────────┘
```

### 지금 당장 모드
```
⚡ 지금 당장 추천 일정

┌─────────────────────────────────────┐
│ 📚 TOEIC 단어 암기 30분              │
│ 추천 이유: 오후 3시는 집중도가 높은 시간│
│                                     │
│          [✅ 지금 바로 시작]         │
└─────────────────────────────────────┘
```

---

## ⚠️ 주의사항

1. **Quick Replies는 3개 이내**
   - 더 많으면 선택 부담 증가
   
2. **추천은 과거 데이터 기반**
   - 신규 사용자는 일반적인 추천
   
3. **"지금 당장" 모드는 부담 없이**
   - 30분-1시간 단위 작은 작업
   - 거절해도 부담 없는 분위기

---

## 📝 TODO

- [ ] AiChatPanel에 Quick Replies 통합
- [ ] generateQuickReplies 함수 구현
- [ ] QuickReplyButtons 스타일 조정
- [ ] 백엔드 Quick Replies API
- [ ] 과거 데이터 수집 로직
- [ ] 추천 알고리즘 구현
- [ ] 탭 UI 디자인
- [ ] "지금 당장" 모드 로직

